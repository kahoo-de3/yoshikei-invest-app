# 開発記録・復旧手順書（FIREを目指そう by よしけい）

このファイルは、本アプリの「作った概要・構成・操作手順・つまずき記録」をまとめたものです。
**PCがクラッシュしてもGitHub上に残る**ように作成しています。万一のときはこの手順で再現できます。

最終更新: 2026-06-19

---

## 1. アプリ概要

- **名称**: FIREを目指そう by よしけい
- **種類**: インデックス投資の参考ダッシュボード（Streamlit製Webアプリ）
- **公開URL**: https://yoshikei-invest-app-ver1.streamlit.app
- **GitHub**: https://github.com/kahoo-de3/yoshikei-invest-app
- **目的**: S&P500を中心に、主要インデックス・市場環境・予測・ニュースを1画面で確認する

### 主な機能
- 上段カード: S&P500 / S&P500先物 / 先物-現物乖離 / NASDAQ100 / SCHD / オルカン / VIX（前日比つき）
- 為替・金利カード: USD/JPY / ドル指数(DXY) / 米国10年債 / 利回り差(10年-3ヶ月)
- VIX・為替/米国債の推移チャート（展開メニュー）
- **Index chart**: S&P500 / NASDAQ100 / SCHD / オルカン のローソク足＋移動平均＋翌日予測
  - 予測（参考シグナル）: アンサンブルML、予測区間、複数日予測、的中履歴、特徴量重要度
- **Index 構成概要**: 各ファンドの組入上位銘柄＋業種構成比率（円グラフ）
- **業種別 パフォーマンス**: 11業種ETFの前日比・期間騰落ランキング（表＋横棒グラフ）
- **資金逃避先（参考資料）**: ディフェンシブ資産の対S&P500相対パフォーマンス
- **金利・株価ニュース**: NHK・Yahoo!ニュース（日本語）＋米国Yahoo Finance（英語＋日本語訳併記）

---

## 2. 技術スタック

- 言語: Python 3.12
- フレームワーク: Streamlit
- グラフ: Plotly (plotly.graph_objects)
- データ取得: yfinance（無料・APIキー不要）
- 予測: scikit-learn（GradientBoosting + RandomForest + ExtraTrees のアンサンブル）
- ニュース: feedparser（RSS）
- ホスティング: Streamlit Community Cloud（GitHubから自動デプロイ）

---

## 3. ファイル構成

| ファイル | 役割 |
|---|---|
| `app.py` | Streamlit UI 本体（画面・レイアウト・テーマ） |
| `data.py` | yfinance データ取得・特徴量生成・ファンド構成データ |
| `model.py` | 翌日〜複数日予測モデル（アンサンブルML） |
| `news.py` | 金利・株価ニュースの RSS 取得・フィルタ |
| `requirements.txt` | 依存パッケージ |
| `.streamlit/config.toml` | ダークテーマ設定 |
| `.gitignore` | Git管理から除外するファイル |
| `start_app.bat` | Windowsローカル起動用バッチ |
| `README.md` / `開発記録.md` | ドキュメント |

---

## 4. ローカルでの起動手順

```powershell
cd C:\Users\kahoo\Downloads\sp500_app
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

デスクトップの「S&P500ダッシュボード」ショートカット（`start_app.bat`）からも起動可能。

---

## 5. PCがクラッシュした場合の復旧手順

アプリ本体・全変更履歴は **GitHub に保存済み**。新しいPCで以下を実行すれば復元できる。

```powershell
# 1. Git と Python をインストール（未導入の場合）
# 2. リポジトリをクローン
git clone https://github.com/kahoo-de3/yoshikei-invest-app.git
cd yoshikei-invest-app

# 3. 依存パッケージをインストール
python -m pip install -r requirements.txt

# 4. 起動
python -m streamlit run app.py
```

- GitHubアカウント: kahoo-de3
- 公開Webアプリ自体は **PCがなくても動き続ける**（Streamlit Cloudが②GitHubから配信しているため）

---

## 6. 変更の反映フロー

```
ローカル編集 → git add/commit → git push → GitHub → Streamlit Cloud が自動再デプロイ
```

```powershell
git add .
git commit -m "変更内容の説明"
git push origin main
```

反映には1〜3分。サイドバー「🔄 データを今すぐ更新」でキャッシュをクリアできる。

---

## 7. Streamlit Cloud デプロイ手順（初回・再設定時）

1. https://share.streamlit.io にログイン（GitHub連携）
2. 「Create app」→「Deploy a public app from GitHub」
3. 設定:
   - Repository: `kahoo-de3/yoshikei-invest-app`
   - Branch: `main`
   - Main file path: `app.py`
4. 「Deploy」

※ リポジトリは **Public** である必要がある（Privateだとドロップダウンに出ない）。

---

## 8. つまずき記録（再発防止メモ）

開発中に遭遇した問題と解決策。同じ問題が起きたら参照する。

| 症状 | 原因 | 解決策 |
|---|---|---|
| チャート背景が暗いまま | Streamlitテーマがplotlyを上書き | `update_layout`で`paper_bgcolor/plot_bgcolor`を明示＋`st.plotly_chart(theme=None)` |
| 横軸ラベルが切れる | マージン不足 | `automargin=True` |
| メトリクスに「なんと」等の語が混入 | ブラウザの自動翻訳 | ブラウザ翻訳をOFF |
| オルカン(2559.T)が異常値 | 株式分割でリターン破綻 | ACWI(USD)×USD/JPY の円換算合成に変更 |
| ニュースが英語のまま | Google翻訳エンドポイントがクラウドIPでブロック | 日本語ネイティブRSS(NHK/Yahoo)に変更。米国分はMyMemory翻訳＋英語併記 |
| 設定変更が反映されない | `st.cache_data`が別モジュールの変更を検知しない | キャッシュキー用のバージョン引数(`_NEWS_VER`等)を用意 |
| リブート後「市場データ取得失敗」 | 冷間起動でyfinanceがレート制限 | `_download`にリトライ＋バックオフ実装 |
| 組入銘柄が取得できない | クラウドで`funds_data`が取得不可 | `data.py`に静的フォールバックデータを内蔵 |
| 見出しの`$`で表示崩れ | Streamlitが`$`をLaTeX数式と誤認 | 表示前に`$`→`\$`にエスケープ |

---

## 9. 設計上のポイント

- **背景テーマ**: 灰色グラデ＋金貨(🪙×15, opacity 0.25)。`inject_theme()`のCSSで制御
- **フォント**: Noto Sans JP に統一
- **見出し**: 主要セクションは1.8rem・中央揃え・ゴールド。`section_title()`等で共通化
- **スマホ最適化**: `MOBILE_CONFIG`でツールバー非表示・スクロール優先。@mediaクエリで縮小
- **データ基準日**: 米国引け(16:00 ET)を日本時間に変換して表示
- **キャッシュ**: 市場データ15分 / ニュース5分（`@st.cache_data` ttl）

---

## 10. 既知の制約・今後の検討事項

- 無料翻訳(MyMemory)はクラウド共有IPで弾かれる時間帯あり（英語のまま表示にフォールバック）
- 予測の実運用ログ（毎日の予測vs実際の乖離蓄積）は**未実装**（永続ストレージが必要なため見送り）
- 組入銘柄の静的フォールバックは2026年6月時点の概算値（最新は各ETF公式参照）
- リブートの多用はキャッシュ全消去→大量フェッチ→レート制限を招くため避ける
